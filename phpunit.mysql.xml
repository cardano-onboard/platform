<?xml version="1.0" encoding="UTF-8"?>
<!--
    CI-only PHPUnit config that runs the suite against MySQL instead of the default
    SQLite (phpunit.xml). MySQL's strict mode (config/database.php `mysql.strict = true`)
    enforces ONLY_FULL_GROUP_BY and other rules that match the RDS/Vapor production DB but
    that SQLite silently ignores — so this catches MySQL-only failures (e.g. aggregate +
    $withCount GROUP BY violations) before they reach staging.

    Connection details (host/port/database/user/password) are taken from the environment
    so CI and local runs can each point at their own MySQL. Run with:
        php artisan test -c phpunit.mysql.xml
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Http/Controllers/Auth</directory>
            <file>app/Providers/BroadcastServiceProvider.php</file>
        </exclude>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_KEY" value="base64:lI6gNEZbi2sItf0uOYVt5Ybi5L2N711OmNIkQNcIZtA="/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <!-- force="true" so this wins over any inherited DB_CONNECTION in the environment. -->
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
